#include <stdio.h>

/**
 *main - print the number of arguments this function is passed
 * @argc:argc
 * @argv: argv
 * Return: zero
 */

int main (argc, argv)
{
	if (argv == 1)
	{
		:
	}
	printf("%d\n", (argc - 1));
	return (0);
}

#include "main.h"
/**
 *_islower - function to determine if a char is lowercase or not
 *Returns - 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	int result;

	result = 0;

	if (c != 0)
	{
		result = 1;
	}
	return (result);
}

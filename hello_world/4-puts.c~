#include <stdio.h>
#include <string.h>
/**
 *prints_to_output - prints a string to output does not use the printf function
 *
 *Return: 0 at all time
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle\n");
return (0);
}

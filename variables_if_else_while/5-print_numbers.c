#include <stdio.h>

/**
 *main - function to print all numbers between 0 and 9
 *Return: zero at all times
 */

int main(void)
{
	char ch = 48;

	for (; ch <= 57; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
